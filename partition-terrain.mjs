import fs from 'node:fs';let s=fs.readFileSync('src/map/World.ts','utf8');s=s.replace("if(m.parent||!m.material||m.name.startsWith('sign-'))continue;", "if(m.parent||!m.material||m.name.startsWith('sign-'))continue;if(m.metadata?.staticChunk){m.freezeWorldMatrix();continue;}");s=s.replace('this.solids=[];for(const pieces', "this.solids=(this.scene.meshes as Mesh[]).filter(m=>m.metadata?.staticChunk&&m.metadata?.solid);for(const pieces");fs.writeFileSync('src/map/World.ts',s);
