import fs from 'node:fs';let s=fs.readFileSync('src/dev/PerformanceMonitor.ts','utf8').replace("button('运行隔离测试',()=>void this.isolate());", "button('运行隔离测试',()=>void this.isolate());button('采样全系统',()=>void this.isolate(['ALL']));");s=s.replace('async isolate(){','async isolate(modes:string[]=[\'ALL\',...Object.keys(this.flags)]){').replace("for(const mode of ['ALL',...Object.keys(this.flags)])",'for(const mode of modes)');s=s.replace('shadowsEnabled:g.scene.shadowsEnabled,flags:', 'renderer:g.engine.getGlInfo(),shadowsEnabled:g.scene.shadowsEnabled,flags:');fs.writeFileSync('src/dev/PerformanceMonitor.ts',s);fs.copyFileSync('src/dev/PerformanceMonitor.ts','../taierzhuang-fps-baseline/src/dev/PerformanceMonitor.ts');
