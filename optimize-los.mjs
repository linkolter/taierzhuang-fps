import fs from 'node:fs';let s=fs.readFileSync('src/map/World.ts','utf8');s=s.replace('    for(const o of this.obstacles){let lo=0,hi=1,hit=true;', '    const candidates=this.collisionIndex?.query(Math.min(a.x,b.x),Math.min(a.z,b.z),Math.max(a.x,b.x),Math.max(a.z,b.z))??this.obstacles;\n    for(const o of candidates){let lo=0,hi=1,hit=true;');fs.writeFileSync('src/map/World.ts',s);
