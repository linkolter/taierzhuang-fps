# Alpha 验收记录

2026-09-09，Windows 桌面环境。使用用户目录中的三张参考图；没有等待或下载游戏美术资产。

## 第二阶段：立体地形与受限拓扑

在现有 Alpha 上扩展，没有重建武器、伤害、据点、Tickets 或比赛系统。按最新要求，闪退根因调查暂缓；下列连续运行结果仅描述本机本次验证，不代表已经解决用户此前遇到的闪退。

- 普通地面 0 m、北侧 +2.4 m、南沟 −1.2 m、地道 −4 m；B 点抬高至 +1.2 m。碰撞、网格和导航共用高度函数，入口孔洞精确到坡道边缘。
- 地上三条纵向战线，**仅有 A/B 间 x=−22 的北↔中连接口，以及 B/C 间 x=22 的中↔南连接口**。隔墙跨到地图边界，没有端头小路。
- 保留原来西/中/东三入口，增加两处高地出口、三处低地出口，共八处。地下宽约 2.4 m，有交错挡墙、支路、低顶、木支撑、油灯和地下小室。
- `TacticalRouteGraph` 扩展原网格，3178 个节点；节点标记高度层与路线，跨层用 stairs / slope / tunnelEntrance 边。29 个可用显式掩体点。
- AI 目标评分加入路线人数、据点威胁、B 点压力和高地敌情；高/低路线经过战术路标，重生后继续正常规划。战斗加入掩体停留、探身、蹲伏、推进和短暂后撤。
- F3 首次创建五个调试线网格，以后复用；显示资源统计和每名 AI 的路线、状态、路径进度、目标据点。

| 当前版本检查 | 结果 |
| --- | --- |
| `npm test` | 5/5 规则测试通过 |
| TypeScript / Vite 构建 | 通过；仍有 Babylon 主包大小提示 |
| 地面拓扑与实体封闭 | 5520 次跨隔墙尝试，含双向、跳跃高度及边界端头；无额外连接 |
| 图拓扑 | 仅两处地面跨线连接；移除它们后，三条地面战线互不连通，地下仍可重新连通全部三线 |
| 地道图边与实体碰撞 | 所有地下/跨层边逐段检查通过 |
| AI 实走路线 | 9/9：地面↔高地、南沟、地下小室、两条高地支路、高地经地下转南沟等 |
| Edge / Chrome 键鼠输入 | 各 10/10；Pointer Lock、移动、蹲、跳、ADS、射击、换弹、大刀切换/挥砍、Esc |
| 玩家跨层通行 | 使用 `Player.update()` 的移动/重力/碰撞走通小室和全部八个出口，无传送过障碍 |
| 战斗与规则浏览器回归 | 17/17，通过头/身体射线、近战、掩体遮挡、占点、死亡重生、胜负与重开 |
| 完整 AI 对局（加速模拟） | 一次记录 531.7 秒日军获胜；455 次射击、90 次阵亡、20 次地下穿行超过 30 m 后出洞 |
| AI 多层使用 | 该局 10 名 AI 到过高地、13 名到过低地、14 名进入地下；记录到 13 种行动状态 |
| 卡住恢复 | 该局共触发 2 次重新寻路，未出现大批角色卡在入口的情况 |
| 100 次重开及全员死亡重生 | 模型身份、导航图实例复用；Mesh 254、材质 41、纹理 18、AI 15、特效池 28、TransformNode 124 全部保持不变 |
| F3 反复切换 | 首次 +5 Mesh，此后 20 次切换无增长 |
| 生产预览 | 点击开始、鼠标锁定、HUD、隐藏菜单通过，生产环境无 `window.__game`，无浏览器异常 |

机器报告：`test-results/topology.json`、`tactical.json`、`tactical-final-gameplay.json`、`input-tunnel-msedge.json`、`input-tunnel-chrome.json`、`lifecycle-tactical.json`。实时连续对局结果写入 `tactical-realtime-15min.json`。

以下为先前平面 Alpha 的历史验收记录；其中 Mesh 和 FPS 数值不代表本次版本。

## 按阶段验证

1. **场景、控制、枪械**：TypeScript 检查通过；Edge 内验证首发扣 1 弹、拉栓期间阻止再次射击、两枪击倒靶标、换弹回到 5 发。浏览器错误为空。
2. **AI**：验证移动、识别敌人、间隔射击以及死亡。修复类型推断问题后通过 TypeScript 和浏览器检查。
3. **完整对局**：验证中国 1+7 / 日军 8、争夺暂停占领、据点扣兵力、躯干与头部伤害、AI/玩家死亡重生、胜利与重开。白盒对局正常达到结算。
4. **村庄与地道**：建立村庄和三入口地道；修复坡道侧墙阻断主通道、入口地面碰撞边缘卡住的问题。使用与游戏相同的碰撞/地面采样连续走过西入口、地下小室、东出口和中央出口，最低高度 -4 m，未使用传送。
5. **最终整合**：制服区分、程序姿态、HUD、占领旗标、合成音效、枪口和命中特效；静态几何按材质/区域合并。修复跳跃被地面贴合逻辑取消和自动化输入下 Esc 未暂停的问题。

## 最终检查

| 检查 | 结果 |
| --- | --- |
| 官方 npm 全新安装 | 成功，17 个依赖包，已生成 `package-lock.json` |
| 项目根目录 npm 安装复核 | 成功 |
| `npm run dev` | 成功，当前运行在 localhost:5173 |
| `npm test` | 5/5 通过 |
| `npm run build` | TypeScript 检查及 Vite 生产构建通过 |
| 生产预览启动 | 点击开始、Pointer Lock、HUD 显示通过；生产环境无调试入口、无浏览器异常 |
| Edge 键鼠输入 | 鼠标锁定、WASD、蹲、跳、ADS、射击、换弹、近战切换/挥砍、Esc：10/10 |
| Chrome 键鼠输入 | 同上 10/10 |
| 战斗规则浏览器检查 | 17/17，包括头/身体射线、近战命中/距离、墙壁遮挡 |
| 地道连续通行 | 西入口 → 主通道 → 地下小室 → 东出口 → 中央出口，通过 |
| 完整 AI 对局 | 最终复核 511.1 秒（8 分 31 秒），中国方 7 : 日军方 0，中国胜利 |
| AI 地道使用 | 最终对局记录到 2 次从入口进入、穿行超过 30 m 并从另一处回到地面的过程 |
| 浏览器错误 | 最终自动化记录为空 |

对局模拟由真实 `Game.step()` 以 0.1 秒步长加速，使用实际 AI、寻路、射线、伤害、重生和据点规则，没有直接指定胜者。另有正常渲染下的真实键盘鼠标输入验收。此前对局也产生过日军胜利，胜者没有固定。

## 性能与范围

- 静态几何合并后场景共约 255 个 mesh，测试视角约 56 个活跃 mesh，包含 15 名 AI、武器及固定特效池。
- 开发机 1920×1080 的短时输入验收约 240fps；这只是本机采样，不代表普通核显长时间稳定 60fps 的保证。
- Vite 提示 Babylon.js 主包较大（约 6 MB 未压缩、1.32 MB gzip）；不影响运行。为避免非核心重构，当前保留稳定的模块导入方式。
- 原型使用低模几何人物/武器、程序动画、合成占位音效；房屋为外壳，暂未制作正式模型、完整骨骼动画和室内探索。
- 此 Codex 环境提供 Node 而没有全局 npm，验收用 `.tools/` 内的官方 npm CLI。正常使用依赖带 npm 的 Node.js 安装；没有改动系统 PATH 或系统设置。

机器生成的详细报告和截图位于 `test-results/`（已加入 `.gitignore`），可用 `README.md` 中的脚本复现。
