# 当前修改与性能诊断交接

更新日期：2026-09-10  
状态：按用户要求停止开发、优化和测试。本文记录停止时的实际状态，不代表最终验收通过。

## 版本与工作区

- 项目：`taierzhuang-fps`。
- 旧版对照 commit：`5b99fa1` — `Playable Alpha 0.1`。
- 当前本地分支：`combat-map-v2`。
- 当前代码仍为未提交的工作区修改，未推送 GitHub。
- 保留旧版分支：`backup/alpha-0.1-5b99fa1`。
- 独立旧版对照工作区：相邻目录 `taierzhuang-fps-baseline`。该副本仅额外加入开发性能监控入口，用于对比；原始代码可由上述 commit 恢复。

## 前一阶段：Combat Map V2 已有改动

- 新增 `src/map/MapLayout.ts`，集中维护 180×90 米地图、A/B/C、路线、建筑、掩体和地道出口布局。
- A/B/C 分别位于约 `(-48,0,0)`、`(0,1,0)`、`(48,0,0)`；出生区位于 X≈±82。
- 建立折弯主街、北侧起伏高地、南侧下沉沟道。
- 地表转线限制为 X≈−14 的北↔中连接，以及 X≈26 的中↔南连接。
- 新增 `TunnelGeometry.ts`，建立有转角、分叉、中央小室的地下网络，共 7 个坡道出入口。
- 地形渲染、移动高度和导航共用高度数据；导航节点同步到当前几何。
- 新增低模环境组件及 7 类程序化 SVG 平铺纹理，位于 `public/textures/map/`。
- 保留原有枪械、伤害、据点、Tickets 和胜负框架；HUD 仅更新地图说明和所在路线文字。
- 新增开发入口 `?mapcheck`，用于玩家移动、指定 AI 路线、对局和视角检查，生产构建不启用。

前一阶段曾记录：玩家通行 11/11、指定 AI 路线 4/4 通过；一次几何阶段的 8v8 加速模拟在约 534.8 秒以中国方获胜结束。这些是历史阶段结果，不能替代本次性能修复后的完整对局验收。

## 本轮性能诊断发现

### 确认的规模变化

| 指标 | 旧 Alpha | 当前新地图 |
| --- | ---: | ---: |
| 碰撞盒 | 449 | 4,887 |
| 导航节点 | 3,178 | 11,211 |
| 有向导航边 | 11,244 | 74,166 |
| 显式掩体点 | 29 | 9 |

新地图扩大了碰撞和导航查询的工作量，而原来的逐项扫描仍在使用。首次 CPU 对比中，同样 120 秒、15 名 AI 的无渲染模拟，旧版进程 CPU 时间约 2.516 秒，当前未优化版本约 14.735 秒。当前版碰撞／地形相关累计耗时约 10.813 秒，为最大的逻辑开销。

未发现每帧重建导航图；该次模拟寻路为 288 次／120 秒，是全体 AI 总数，并非 15 名 AI 每帧寻路。掩体搜索不是主要瓶颈。当前游戏没有创建动态 ShadowGenerator，不能把禁用阴影后的采样波动解释为已确认的阴影瓶颈。

### 已加入的诊断工具

- `src/dev/PerformanceMonitor.ts`：使用 `?perf` 开启，逐秒显示 FPS、帧间隔、CPU 单帧耗时、AI、寻路、最近节点查询、LOS、掩体、碰撞／地形、视觉网格射线与渲染耗时。
- 同时显示 mesh、active mesh、材质、纹理、draw calls、active indices、AI、waypoint、cover 数量、每秒射线与寻路次数、实际渲染尺寸和显卡信息。
- 提供 `DISABLE_AI`、`DISABLE_PATHFINDING`、`DISABLE_PERCEPTION`、`DISABLE_COVER`、`DISABLE_COLLISIONS_TEST`、`DISABLE_SHADOWS` 开关。
- 隔离测试以固定随机种子和固定出生轮次预热至同一战斗阶段，预热不计入结果。
- `scripts/profile-cpu.ts`：使用 Babylon NullEngine 和实际 World/Bot/Combat/Capture/Match 代码做 CPU 对比；它不测 GPU，也不代表实际渲染 FPS。
- 计时区分自身耗时与包含子调用的耗时，不能将 inclusive 数据直接相加。

## 本轮已经实施的性能修复

### 1. 碰撞查询空间索引

新增 `src/map/ObstacleIndex.ts`，以 4 米网格索引原有碰撞盒。

- 地图建成后创建索引；后续经 `World.box()` 添加的实体也会加入索引。
- `World.canStand()` 只检查附近候选盒，保留原有精确碰撞条件。
- 没有移除碰撞盒、放宽通行条件或改变地图高度。
- 该阶段碰撞／地形累计耗时由约 10.813 秒降至约 0.852 秒。

### 2. 地形视觉网格分块

修改 `src/map/Terrain.ts` 与 `World.optimizeStatic()`。

- 将地形分为 30×15 米的静态区域，避免重新合并为全图大网格。
- 保留原来的网格密度、三角形、UV、高度和地道孔洞；区块边缘会共享位置但重复存储顶点。
- 让视锥裁剪和射线包围盒检测跳过无关区块。
- 同一阶段对比中，视觉网格射线累计耗时由约 3.351 秒降至约 0.982 秒。

### 3. LOS 候选范围缩小

`World.blocked()` 使用相同空间索引选择射线覆盖范围内的碰撞盒，仍执行原有精确线段相交和地形检查。

- 该阶段 LOS 累计耗时由前一阶段约 1.063 秒降至约 0.276 秒。

### 4. 导航查询与分配优化

修改 `src/map/TacticalRouteGraph.ts`。

- 最近 waypoint 查询使用静态空间分桶，保留原来的距离与可通行性检查。
- 等距节点按原节点 ID 顺序选择，避免无谓改变结果。
- A* 堆改为复用数字数组，减少每次入堆创建临时对象。
- 增加最多 128 项的路线模板缓存；返回独立路径数组，节点位置仍由静态图持有。
- 当前缓存基于地图静态这一前提；若以后加入动态障碍或重建图，必须处理缓存失效。

## 停止前最后一次 CPU 测量

相同脚本的 120 秒模拟；不同批次会受进程调度、JIT 和 GC 波动影响，不应将这些数据当作整机 CPU 占用率。

| 指标 | 最初未优化新地图 | 停止前最后一次 |
| --- | ---: | ---: |
| 进程 CPU 时间 | 14.735 s | 4.141 s |
| 模拟总耗时 | 14.064 s | 2.807 s |
| 平均逻辑帧耗时 | 1.953 ms | 0.389 ms |
| P95 逻辑帧耗时 | 3.401 ms | 0.739 ms |
| 最大逻辑帧耗时 | 1,113.820 ms | 180.982 ms |
| 碰撞／地形累计自身耗时 | 10.813 s | 0.495 s |
| 最近节点查询累计自身耗时 | 0.399 s | 0.028 s |
| 全体 AI 寻路次数 | 288 | 288 |

平均开销明显下降，但最大帧峰值仍然过高，性能修复尚未完成。

## 浏览器测量及限制

- 实测环境：1920×1080，Intel UHD Graphics，ANGLE Direct3D11。
- 未优化新地图初次浏览器全系统采样约 33.3 FPS；固定状态校准后的另一批采样约 39.6 FPS、平均 CPU 帧耗时 9.71 ms、峰值 50.5 ms。
- 旧 Alpha 在同一 1080p 环境的一次采样约 39.3 FPS、平均 CPU 帧耗时 9.98 ms、峰值 18.2 ms。
- 本机旧版本身也未在该采样下达到稳定 60 FPS，因此不能仅据 FPS 宣称回归已修复或达到用户目标。
- 尚未完成全部最新修复后的最终 1080p 浏览器复测、整机 CPU 占用测量和完整 8v8 对局。
- 没有降低分辨率、纹理质量、地图几何精度、AI 人数或关闭正式游戏中的系统来掩盖问题。

## 最新通过的检查

停止前最近一次测试为 **14/14 通过**：

- 新增空间索引候选完整性测试：随机边界、跨网格对象、后续新增对象。
- 三线各自地表贯通、两个转线口、隔墙站立／蹲伏／跳跃阻挡。
- 地下导航边、7 个坡道、据点坐标、高度、出生保护遮挡。
- 主街视距抽样、高地不能同时覆盖 A/B/C、掩体位置。
- 原有占点、地下不能穿地占领、Tickets、胜负和重置规则。
- 最新 TypeScript 检查通过。

尚未运行最新修复状态的最终生产 build。不要把早期的 build 或通行结果当成本次最终验收。

## 已定位但尚未修复／验证的项目

1. **出生和据点事件的 AI 寻路错峰没有完整生效。** `Bot.respawn()` 将 `replanAt` 设为 0；`Bot.update()` 对 `dirty` 的处理未检查该时间，且另一分支可在首帧触发规划。因此出生首帧可能集中规划 15 名 AI。停止前只完成定位，尚未修改调度代码。
2. 最大帧峰值仍存在。最后一次峰值记录包含出生首帧约 80 ms 的单次寻路，以及约 28 ms 的视觉网格射线调用。
3. 需要在最新代码上重新运行固定状态的浏览器隔离测试，核实平均与峰值 CPU 帧耗时。
4. 需要验证出生、占点变化、死亡重生、完整 8v8 对局及实时帧率；最终目标仍为 CPU 帧预算 <16.6 ms、AI 平均约 3–5 ms 以内，并在适合的硬件上验证稳定 60 FPS。
5. 若继续修改静态图或添加动态障碍，需检查路线缓存失效机制。
6. 性能脚本中的本机 canvas 依赖路径需要整理后才适合跨机器复用。
7. 工作区仍有本轮和此前使用的 `fix-*.mjs`、`optimize-*.mjs`、探查脚本等临时文件，尚未清理，也未整理最终提交。

## 数据与复现

机器生成的 JSON 位于被 Git 忽略的 `test-results/`，阶段报告位于 `test-results/performance/`，其中保存有原版对照、优化前、碰撞优化后、地形分块后、LOS 优化后等记录。`current-before.zip` 是性能优化前源码快照。

开发命令（在项目目录）：

```text
pnpm dev
pnpm test
pnpm build
```

浏览器开发入口：

```text
http://127.0.0.1:5173/?perf
http://127.0.0.1:5173/?mapcheck
```

CPU 对比脚本：

```text
pnpm exec tsx scripts/profile-cpu.ts . all
pnpm exec tsx scripts/profile-cpu.ts ../taierzhuang-fps-baseline all
```

注意：CPU 脚本会加速模拟，不应把其运行时的高 CPU 或渲染外采样解释为真实实时对局帧率。浏览器隔离测试的预热阶段也会加速模拟，必须排除该阶段后读取结果。
