import fs from 'node:fs';let s=fs.readFileSync('scripts/profile-cpu.ts','utf8').replace('const times:Record', 'const slowCalls:any[]=[];\nconst times:Record');s=s.replace('s.ms+=elapsed-child;s.calls++;','s.ms+=elapsed-child;s.calls++;if(elapsed>15)slowCalls.push({bucket,key,ms:elapsed,time});');s=s.replace('meshes:scene.meshes.length,cover:', 'slowCalls:slowCalls.sort((a,b)=>b.ms-a.ms).slice(0,20),meshes:scene.meshes.length,cover:');fs.writeFileSync('scripts/profile-cpu.ts',s);
